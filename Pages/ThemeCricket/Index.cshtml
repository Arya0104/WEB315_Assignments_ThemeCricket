@page
@model WEB315_Assignments_ThemeCricket.Pages_ThemeCricket.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ThemeCricket[0].StadiumName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ThemeCricket[0].BookingDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ThemeCricket[0].City)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ThemeCricket[0].State)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ThemeCricket[0].Price)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.ThemeCricket) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StadiumName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BookingDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.City)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.State)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
